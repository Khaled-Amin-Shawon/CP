#include<stdio.h>
int main()
{
    int testCase;
    scanf("%d",&testCase);
    for (int  i = 0; i < testCase; i++)
    {
        int H,M,n;
        scanf("%d%d%d",&n,&H,&M);
        int hour[n],minute[n];
        for ( int k = 0; k < n; k++)
        {
            scanf("%d%d",&hour[k],&minute[k]);
        }
        for (int l = 0; l < n; l++)
        {
           
        }
        
        
        
    }
    
}